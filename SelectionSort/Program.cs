using static Sorting;
using static Infrastructure;

// int[] array = CreateArray(10);
// Show(array);
// SortSelection(array);
// Show(array);

8.CreateArray(min: 0, max: 5)
  .Show()
  .SortSelection()
  .Show();

